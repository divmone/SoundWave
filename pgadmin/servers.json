{
    "Servers": {
        "1": {
            "Name": "Auth DB",
            "Group": "Servers",
            "Host": "auth-postgres",
            "Port": 5432,
            "MaintenanceDB": "${AUTH_DB_NAME}",
            "Username": "${POSTGRES_USER}",
            "PassFile": "${POSTGRES_PASSWORD}"
        },
        "2": {
            "Name": "Sounds DB",
            "Group": "Servers",
            "Host": "sounds-postgres",
            "Port": 5432,
            "MaintenanceDB": "${SOUNDS_DB_NAME}",
            "Username": "${POSTGRES_USER}",
            "PassFile": "${POSTGRES_PASSWORD}"
        },
        "3": {
          "Name": "Payment DB",
          "Group": "Servers",
          "Host": "payment-postgres",
          "Port": 5432,
          "MaintenanceDB": "${PAYMENT_DB_NAME}",
          "Username": "${POSTGRES_USER}",
          "PassFile": "${POSTGRES_PASSWORD}"
        },
        "4": {
          "Name": "Crypto Payment DB",
          "Group": "Servers",
          "Host": "crypto-payment-postgres",
          "Port": 5432,
          "MaintenanceDB": "${CRYPTO_PAYMENT_DB_NAME}",
          "Username": "${POSTGRES_USER}",
          "PassFile": "${POSTGRES_PASSWORD}"
        }
    }
}